
import { posts } from "@/data/posts";
import type { BlogPost } from "@/types/types";

export function getRecentBlogPosts(): BlogPost[] {
  // Replace this mock data with actual data fetching logic
    return posts;
}